export enum TextBoxType {
    default = 'default',
    hasButton = 'hasButton',
}

export enum InputType {
    text = 'text',
    color = 'color',
    date = 'date',
    datetime = 'datetime',
    email = 'email',
    file = 'file',
    image = 'image',
    number = 'number',
    passwords = 'password',
    datetimeLocal = 'datetime-local',
}
